---
import Layout from '../layouts/Layout.astro'

const examples = [
   'minecraft/diamond',
   'minecraft/acacia_boat',
   'minecraft/diamond',
   'minecraft/bookshelf',
   'quark/dark_oak_bookshelf',
]

const exampleUrls = examples.map(it => `/icons/${it}.png`)
---

<Layout title="Minecraft Icons">
   <main>
      <h1>Minecraft Icons</h1>
      <p>This website serves icons of minecraft blocks & items that have been generated using its assets.</p>
      <p>Try requesting an icons using the following path template:</p>
      <code>/assets/icons/[namespace]/[id].png</code>
      <p>For example:</p>
      <ul>
         {
            exampleUrls.map(url => (
               <li>
                  <a href={url}>{url}</a>
               </li>
            ))
         }
      </ul>

      <a href="/browse">
         <button class="accent">See all available icons</button>
      </a>

      <a href="/mods">
         <button>Supported mods</button>
      </a>
   </main>
</Layout>

<style>
   main {
      max-width: 1000px;
      margin: 0 auto;
   }

   li:not(:last-child) {
      margin-bottom: 0.5em;
   }

   button {
      margin-top: 1rem;
      margin-left: 1rem;
      min-width: 160px;;

      &.accent {
         background: var(--accent);

         &:hover {
            background: var(--accent-light);
            color: black;
         }
      }
   }
</style>
